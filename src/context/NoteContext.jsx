// first import creat context than assign it some variable then export now i need to basically make a state 
// in this app all context related to note is written here 
import { createContext } from "react";
const NoteContext=createContext();
export default NoteContext;